<UserControl x:Class="MarioKart.Desktop.Views.Races"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MarioKart.Desktop.Views"
             mc:Ignorable="d" Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid Name="dgRaces" AutoGenerateColumns="False" ColumnWidth="*" CanUserAddRows="false" IsReadOnly="True" AlternatingRowBackground="AliceBlue">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding ID}" />
                <DataGridTextColumn Header="Race Date" Binding="{Binding RaceDate}" />
                <DataGridTextColumn Header="Tournament Name" Binding="{Binding TournamentName}" />
                <DataGridTextColumn Header="Grand Prix Name" Binding="{Binding GrandPrixName}" />
                <DataGridTextColumn Header="Driver" Binding="{Binding Driver}" />
                <DataGridTextColumn Header="Position" Binding="{Binding Position}" />
                <DataGridTextColumn Header="Points" Binding="{Binding Points}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
